{
  "name": "bryson-benjamin-portfolio",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "dev:web": "bun run --cwd apps/web dev",
    "dev:api": "bun run --cwd apps/api dev",
    "build": "bun run --cwd packages/sa-sa-contracts build && bun run --cwd packages/portfolio-content build && bun run --cwd packages/db build && bun run --cwd apps/api build && bun run --cwd apps/web build",
    "build:api": "bun run --cwd packages/sa-sa-contracts build && bun run --cwd packages/portfolio-content build && bun run --cwd packages/db build && bun run --cwd apps/api build",
    "start:api": "bun run --cwd apps/api start",
    "typecheck": "bun run --cwd packages/sa-sa-contracts typecheck && bun run --cwd packages/portfolio-content typecheck && bun run --cwd packages/db typecheck && bun run --cwd apps/api typecheck && bun run --cwd apps/web typecheck",
    "db:generate": "bun run --cwd packages/db db:generate",
    "db:migrate": "bun run --cwd packages/db db:migrate",
    "db:studio": "bun run --cwd packages/db db:studio",
    "docs:prepare-sync": "bun scripts/prepareDocsSync.ts"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
